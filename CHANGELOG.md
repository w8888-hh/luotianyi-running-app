# 更新日志

本项目版本号跟随安卓 APK 的 `versionName`（`android/app/build.gradle`）。

## v1.2

> 这是发布到 GitHub 后的第一个大版本更新，重点是**跑法更聪明、音乐更好用、真机不再闪退**。

### 新增

- **第三种跑法：自定义 BPM**
  自己定一个固定步频，节拍器全程按这个值走 —— 不分阶段、不随心率变速、不自动结束，跑多久你说了算。
  设定范围 60–220 BPM，带 6 个常用档位（轻松 150 / 慢跑 160 / 节奏 170 / 快跑 180 / 冲刺 190 / 间歇 200）。
  跑步页顶部有步频胶囊，**跑的过程中点一下就能改**，觉得快了慢了随时调，改完当拍生效。

- **引导跑：四阶段占比可调**
  总时长仍然等于歌单总长，但四个阶段各占多少由你决定。改一根滑块，其余三根自动按比例重分，合计恒为 100%。

- **自定义跑的节拍器音色可选**
  均衡器面板底部新增「节拍器音色」：**木块**（默认，噪声瞬态 + 共鸣体合成，像打击乐）/ **电子**（原方波，刺耳但户外穿透力强）。点一下立刻示范两拍。

- **常驻通知条**
  跑步时锁屏和通知栏都能看到实时数据（时长 / 距离 / 配速 / 心率 / BPM / 阶段 + 当前曲目），
  带五个图标按钮：上一首、播放暂停、下一首、暂停·开始运动、结束运动。图标随状态切换，跟播放状态实时同步。

- **已存歌单的详情页**
  点歌单不再直接载入播放，而是先进详情页看封面、名称、歌手、时长，再决定「载入并播放」还是「加入播放列表」。

- **运动结束存歌单**
  跑完会问你要不要把这次听过的歌存成歌单，按日期命名存在「我的歌单」里。

- **批量导入**
  内置专辑里长按一首进入多选，可以一次把整张专辑加进来。

### 改进

- **跟随跑的节拍器改为渐进引导**
  以前换心率区间时步频一步跳十几拍，跑起来很蹩脚。现在每秒最多变 2.5 拍，慢慢把你带到该区间的标准步频；
  你加速它就跟着加一点，你稳住它就不动，到位后完全稳定不抖。跑步页会显示 `BPM 165 → 172`，让你知道天依正把你往哪个节奏带。

- **开跑时音乐跟节拍器一起起来**
  以前要暂停再继续才会出声，现在节拍器一响音乐立刻跟上。

- **批量导入大幅提速**
  分块 512KB → 2MB、按时间片让出主线程（不再被 4ms clamp 拖住）、读写预读流水线、多文件并发 2、
  元数据批量落盘、导入期间不抢主线程测 BPM。实测整批快 58%–78%（40MB 单文件 3.7s → 1.6s）。

- **阶段时间提示**（引导跑）
  实时显示当前是第几阶段、本阶段还剩多久、距下一阶段还有多久、全程还剩多久。

- **导入的音频会持久化**
  重启 App 不用重新导入；不需要的歌可以「移除」（只出歌单，不删本地文件），之后还能加回来。

- **语音探测结果缓存**
  以前每句话都要先探测一遍音频扩展名（第一条 mp3 必然 404），天依会慢半拍开口。现在探过一次就记住。

### 修复

- **点击开始跑步后闪退**（Android 14）
  两个坑叠加：Manifest 声明了 `foregroundServiceType="mediaPlayback"`，但代码调的是两参数 `startForeground()`，
  Android 14 会抛 `MissingForegroundServiceTypeException`；异常被 catch 后走 `NotificationManager.notify()` 兜底，
  而这行没包 try，Android 13+ 没通知权限时直接抛 `SecurityException` 崩进程。
  现在改为传 type 的三参数调用、没权限就绝不贴通知、所有兜底路径都吞异常、
  前台起不来时主动 `stopSelf()`（否则系统会在几秒后判定「调了 startForegroundService 却没成功」杀掉进程）。

- **节拍器音色太机械**
  原来只有一个方波振荡器（单频 + 50ms 衰减），是纯电子「哔」。
  改成噪声瞬态（过带通）+ 三角波共鸣（起音频率下滑）+ 低通三层合成，听感接近木鱼/木块。

- 通知条布局里的分隔线从裸 `View` 换成 `ImageView` —— RemoteViews 的控件白名单不含 `android.view.View`，
  部分 ROM 上会 inflate 失败导致通知条出不来。

## v1.0

首个公开版本。节拍器 + 心率监测 + 音乐播放 + 洛天依语音引导，浏览器版与安卓 APK 同时提供。
